
# 从列表创建元组
tuple1 = tuple([x for x in range(10)])
# 从元组创建列表
list1 = list[(1, 2, 3)]

# 创建一个空集合,注意set1={}是创建的字典
set1 = set()

# 创建一个字符串和数字的列表
list2 = [1, 2, 3, "one", "two", "three"]
# 创建一个字符串和数字的集合
set2 = {1, 2, 3, "one", "two", "three"}
# 创建一个字符串和数字的元组
tuple2 = (1, 2, 3, "one", "two", "three")

# 从列表创建集合
set3 = {list[1, 2, 3]}
# 从集合创建列表
list3 = [{1, 2, 3}]
# 从集合创建元组
tuple3 = tuple({1, 2, 3})

# 创建一个空字典
dict1 = {}
